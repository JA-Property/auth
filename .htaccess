<IfModule mod_rewrite.c>
  RewriteEngine On
  # If the requested resource doesn't exist as a file, route it to index.php
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^.*$ index.php [QSA,L]
</IfModule>

# Prevent direct access to the .env file
<Files ".env">
    Require all denied
</Files>
